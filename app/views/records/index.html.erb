<h1>つぶやき一覧</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @records.each do |record| %>
  <tr>
    <td><%= record.content %></td>
    <td><%= link_to "記録を編集する", edit_record_path(record.id) %></td>
  </tr>
<% end %>

</table>

<%= link_to '新しくブログを作成する', new_record_path %>